apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-navidrome
  namespace: cloudflared
spec:
  selector:
    matchLabels:
      app: cloudflared-navidrome
  template:
    metadata:
      labels:
        app: cloudflared-navidrome
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - pi-node
      tolerations:
        - key: workload
          operator: Equal
          value: stateless
          effect: NoSchedule
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:2026.3.0
          securityContext:
            capabilities:
              add:
                - NET_ADMIN
          resources:
            requests:
              cpu: 20m
              memory: 32Mi
            limits:
              cpu: 500m
              memory: 64Mi
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: navidrome-tunnel-token
                  key: token
          command:
            - cloudflared
            - tunnel
            - --no-autoupdate
            - run
          livenessProbe:
            exec:
              command:
                - cloudflared
                - tunnel
                - --version
            initialDelaySeconds: 10
            periodSeconds: 30
            failureThreshold: 3
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: navidrome-tunnel-token
  namespace: cloudflared
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-secretsmanager
  target:
    name: navidrome-tunnel-token
    creationPolicy: Owner
  data:
    - secretKey: token
      remoteRef:
        key: cloudflared/navidrome-tunnel-token/token
