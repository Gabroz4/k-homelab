apiVersion: batch/v1
kind: CronJob
metadata:
  name: immich-files-backup
  namespace: immich
spec:
  schedule: "0 3 */3 * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 300
      activeDeadlineSeconds: 14400
      backoffLimit: 1
      template:
        spec:
          restartPolicy: OnFailure
          affinity:
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
              - labelSelector:
                  matchLabels:
                    app: immich-server
                topologyKey: kubernetes.io/hostname
          containers:
          - name: backup
            image: restic/restic
            resources:
              requests:
                cpu: 100m
                memory: 128Mi
              limits:
                cpu: 500m
                memory: 512Mi
            command:
            - /bin/sh
            - -c
            - |
              set -euo pipefail
              restic -r /backup/immich-files snapshots 2>/dev/null || restic -r /backup/immich-files init
              restic -r /backup/immich-files backup /data
              restic -r /backup/immich-files forget --keep-daily 7 --keep-weekly 4 --prune
            env:
            - name: RESTIC_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: backup-secrets
                  key: restic-password
            volumeMounts:
            - name: data
              mountPath: /data
              readOnly: true
            - name: backup
              mountPath: /backup
          volumes:
          - name: data
            persistentVolumeClaim:
              claimName: immich-library
          - name: backup
            hostPath:
              path: /mnt/backups
              type: Directory
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: immich-db-backup
  namespace: immich
spec:
  schedule: "20 3 */3 * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 300
      activeDeadlineSeconds: 3600
      backoffLimit: 1
      template:
        spec:
          restartPolicy: OnFailure
          containers:
          - name: backup
            image: postgres:16
            resources:
              requests:
                cpu: 50m
                memory: 64Mi
              limits:
                cpu: 200m
                memory: 256Mi
            command:
            - /bin/sh
            - -c
            - |
              set -euo pipefail
              OUTFILE="/backup/immich-db-$(date +%F).sql.gz"
              pg_dump -U immich -h immich-postgresql immich | gzip > "$OUTFILE.tmp" && mv "$OUTFILE.tmp" "$OUTFILE"
              echo "Dump written to $OUTFILE"
              find /backup -name "immich-db-*.sql.gz" -mtime +7 -delete
            env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: immich-secrets
                  key: POSTGRES_PASSWORD
            volumeMounts:
            - name: backup
              mountPath: /backup
          volumes:
          - name: backup
            hostPath:
              path: /mnt/backups
              type: Directory
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: nextcloud-db-backup
  namespace: nextcloud
spec:
  schedule: "40 3 */3 * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 300
      activeDeadlineSeconds: 3600
      backoffLimit: 1
      template:
        spec:
          restartPolicy: OnFailure
          containers:
          - name: backup
            image: postgres:17
            resources:
              requests:
                cpu: 50m
                memory: 64Mi
              limits:
                cpu: 200m
                memory: 256Mi
            command:
            - /bin/sh
            - -c
            - |
              set -euo pipefail
              OUTFILE="/backup/nextcloud-db-$(date +%F).sql.gz"
              pg_dump -U nextcloud -h nextcloud-postgresql nextcloud | gzip > "$OUTFILE.tmp" && mv "$OUTFILE.tmp" "$OUTFILE"
              echo "Dump written to $OUTFILE"
              find /backup -name "nextcloud-db-*.sql.gz" -mtime +7 -delete
            env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: nextcloud-db
                  key: db-password
            volumeMounts:
            - name: backup
              mountPath: /backup
          volumes:
          - name: backup
            hostPath:
              path: /mnt/backups
              type: Directory
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: nextcloud-minio-backup
  namespace: nextcloud
spec:
  schedule: "0 4 */3 * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 300
      activeDeadlineSeconds: 7200
      backoffLimit: 0
      template:
        spec:
          restartPolicy: Never
          containers:
          - name: backup
            image: minio/mc:RELEASE.2024-11-17T19-35-25Z
            resources:
              requests:
                cpu: 100m
                memory: 128Mi
              limits:
                cpu: 500m
                memory: 1Gi
            env:
            - name: MINIO_ENDPOINT
              value: "http://minio.minio.svc.cluster.local:9000"
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-secret
                  key: accessKey
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-secret
                  key: secretKey
            command:
            - /bin/sh
            - -c
            - |
              set -euo pipefail
              mc alias set minio "$MINIO_ENDPOINT" "$MINIO_ACCESS_KEY" "$MINIO_SECRET_KEY"
              TARGET="/backups/nextcloud/data"
              mkdir -p "$TARGET"
              echo "== Mirroring minio/nextcloud -> $TARGET =="
              mc mirror --overwrite minio/nextcloud "$TARGET"
              echo "== Done =="
            volumeMounts:
            - name: backups
              mountPath: /backups
          volumes:
          - name: backups
            hostPath:
              path: /mnt/backups
              type: Directory
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: authentik-db-backup
  namespace: authentik
spec:
  schedule: "0 5 */3 * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 300
      activeDeadlineSeconds: 3600
      backoffLimit: 1
      template:
        spec:
          restartPolicy: OnFailure
          containers:
          - name: backup
            image: postgres:17
            resources:
              requests:
                cpu: 50m
                memory: 64Mi
              limits:
                cpu: 200m
                memory: 256Mi
            command:
            - /bin/bash
            - -c
            - |
              set -euo pipefail
              OUTFILE="/backup/authentik-db-$(date +%F).sql.gz"
              pg_dump -U authentik -h authentik-postgresql authentik | gzip > "$OUTFILE.tmp" && mv "$OUTFILE.tmp" "$OUTFILE"
              echo "Dump written to $OUTFILE"
              find /backup -name "authentik-db-*.sql.gz" -mtime +7 -delete
            env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: authentik-credentials
                  key: postgresql-password
            volumeMounts:
            - name: backup
              mountPath: /backup
          volumes:
          - name: backup
            hostPath:
              path: /mnt/backups
              type: Directory
